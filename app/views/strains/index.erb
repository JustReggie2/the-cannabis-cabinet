<a href="/myaccount">Home</a>
<h1>Welcome to the Strain Library</h1>
<p>
  <h2>Here you can browse different strains and add them to your cabinet.
    Click on any strain to learn more. Happy collecting</h2>
</p>

<div class="">
  <h2>Hybrid:</h2>
  <% @strains.each do |strain| %>
    <% if strain.category.downcase == "hybrid" %>
      <a href="/strains/<%= strain.slug %>"><%= strain.name %></a>
    <% end %>
  <% end %>
</div>

<div class="">
  <h2>Indica:</h2>
  <% @strains.each do |strain| %>
    <% if strain.category.downcase == "indica" %>
      <a href="/strains/<%= strain.slug %>"><%= strain.name %></a>
    <% end %>
  <% end %>
</div>

<div class="">
  <h2>Sativa:</h2>
  <% @strains.each do |strain| %>
    <% if strain.category.downcase == "sativa" %>
      <a href="/strains/<%= strain.slug %>"><%= strain.name %></a>
    <% end %>
  <% end %>
</div>
